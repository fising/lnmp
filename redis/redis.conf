# Redis 7 配置
bind 0.0.0.0
port 6379

# 持久化（RDB）
save 900 1
save 300 10
save 60 10000

# AOF
appendonly yes
appendfsync everysec

# 内存限制
maxmemory 256mb
maxmemory-policy allkeys-lru

# 慢查询日志
slowlog-log-slower-than 10000
slowlog-max-len 128

# 密码留空 — 开发环境不设密码
